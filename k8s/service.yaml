apiVersion: v1
kind: Service
metadata:
  name: paperless-mcp
  labels:
    app: paperless-mcp
spec:
  type: ClusterIP
  selector:
    app: paperless-mcp
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
